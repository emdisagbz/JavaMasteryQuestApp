<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Floor Level</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900&display=swap');
        body {
            font-family: Poppins;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-image: url("BG1.jpg");
            overflow: hidden;
        }
        .grid-container {
            display: grid;
            grid-template-columns: repeat(1, 400px);
            height: 30vh;
            gap: 10px;
        }
        .grid-item {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: orange;
            border-radius: 5px;
            color: white;
            text-align: center;
            font-size: 24px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }
        .grid-item .floor {
            font-size: 28px;
        }
        .grid-item .method {
            font-size: 20px;
        }
        .grid-item:hover {
            background-color: red;
            transform: scale(0.95);
        }
        #backButton {
            font-family: Poppins;
            position: absolute;
            top: 10px;
            left: 20px;
            width: 250px;
            padding: 15px;
            font-size: 20px;
            color: white;
            background: linear-gradient(to right, #007bff 0%, #007bff 0%, rgba(0, 123, 255, 0) 75%);
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        #backButton:hover {
            background-color: #0056b3;
            background: linear-gradient(to right, #007bff 0%, #007bff 50%, rgba(0, 123, 255, 0) 75%);
            transform: scale(0.95);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            background-color: rgba(0,0,0,0.4);
        }

        .modal-content {
            background-color: #fefefe;
            margin: 10% auto;
            margin-top: 7%;
            padding: 20px;
            border: 1px solid #888;
            width: 90%; /* Adjusted width */
            max-width: 600px; /* Adjusted max-width */
            border-radius: 10px;
            text-align: left;
        }

        .modal-content h2 {
            text-align: center;
            margin: 0 0 10px;
        }

        .modal-content h4 {
            text-align: center;
            margin-bottom: 20px;
        }

        .modal-content ol {
            margin: 10px 0;
            padding-left: 20px;
        }

        .modal-content ol li {
            margin: 10px 0;
        }

        .modal-buttons {
            display: flex;
            justify-content: space-around;
            margin-top: 20px;
        }

        .modal-button {
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .modal-button.go {
            background-color: #007bff;
            color: white;
        }

        .modal-button.go:hover {
            background-color: #0056b3;
            transform: scale(0.95);
        }

        .modal-button.back {
            background-color: #ccc;
            color: black;
        }

        .modal-button.back:hover {
            background-color: #aaa;
            transform: scale(0.95);
        }
    </style>
</head>
<body>

<div class="grid-container">
    <div class="grid-item" onclick="openModal(1)">
        <div class="floor">Floor 1</div>
        <div class="method">Elements of Java</div>
    </div>
    <div id="backButton">Back to Part Selection</div>
</div>

<div id="modal" class="modal">
    <div class="modal-content">
        <h2>Instructions</h2>
        <h4 id="floorTitle"></h4>
        <ol>
            <li><b>Question Types:</b> There are 3 types (Identification, Multiple Choice, Complete the Code) with different point values (3, 2, 5 points each).</li>
            <li><b>Time Limits:</b> Each question type has a time limit (15 seconds, 10 seconds, 45 seconds).</li>
            <li><b>Lives:</b> You get 3 tries per question. Lose all 3 tries, lose 1 point.</li>
            <li><b>Hints:</b> Get 3 hints per question. 1st hint is free, 2nd & 3rd cost 100 silver coins.</li>
            <li><b>Rewards:</b> Earn 100 silver coins for each correct answer and 100 gold coins for finishing this floor.</li>
        </ol>
        <div class="modal-buttons">
            <button id="goButton" class="modal-button go">Go</button>
            <button id="closeButton" class="modal-button back">Back</button>
        </div>
    </div>
</div>

<script>
    function openModal(level) {
        document.getElementById("modal").style.display = "block";
        updateFloorTitle(level);
    }

    function updateFloorTitle(level) {
        let title = "Java Programming";
        document.getElementById("floorTitle").textContent = title;
    }

    document.getElementById("goButton").addEventListener("click", function() {
        let url = "BW1P1F1Games.html";
        window.location.href = url;
    });

    document.getElementById("closeButton").addEventListener("click", function() {
        document.getElementById("modal").style.display = "none";
    });

    document.getElementById("backButton").addEventListener("click", function() {
        window.location.href = "BW1Part.html"; // Change this to the appropriate URL
    });
</script>

</body>
</html>